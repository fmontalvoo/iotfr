name: iotfr_app
description: Aplicación para la tesis.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3

  # Dependecias de fcm
  firebase_messaging: ^7.0.0
  # URL launcher
  url_launcher: ^5.7.5

  http: ^0.12.2

  shared_preferences: ^0.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.7.5

flutter_icons:
  image_path: 'assets/icono.png'
  android: true
  ios: true


flutter:

  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/qualtronic.png
